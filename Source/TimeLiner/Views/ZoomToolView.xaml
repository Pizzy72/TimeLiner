<?xml version="1.0" encoding="utf-8"?>
<!--
SPDX-License-Identifier: MIT
Copyright (c) 2021-2025 Christian Pistor
-->
<UserControl
    x:Class="TimeLiner.Views.ZoomToolView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="200"
    d:DesignWidth="400"
    mc:Ignorable="d">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <Canvas Visibility="{Binding Path=IsVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
        <Rectangle
            Canvas.Left="{Binding Path=Left}"
            Width="{Binding Path=Width}"
            Height="{Binding Path=Height}"
            Fill="{DynamicResource ZoomToolBrush}"
            Opacity="0.1" />
    </Canvas>
</UserControl>
