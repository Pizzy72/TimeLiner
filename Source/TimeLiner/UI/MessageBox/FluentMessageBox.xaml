<?xml version="1.0" encoding="utf-8"?>
<!--
SPDX-License-Identifier: MIT
Copyright (c) 2021-2025 Christian Pistor
-->
<ui:BaseRibbonWindow
    x:Class="TimeLiner.UI.MessageBox.FluentMessageBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:f="urn:fluent-ribbon"
    xmlns:ui="clr-namespace:TimeLiner.UI"
    Title="{Binding DialogTitle}"
    SizeToContent="WidthAndHeight"
    WindowStartupLocation="CenterOwner"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    KeyDown="Window_KeyDown"
    >

    <ui:BaseRibbonWindow.Resources>
        <Style TargetType="Button"
               BasedOn="{StaticResource DialogButtonStyle}"
               >
            <Setter Property="Width" Value="80"/>
            <Setter Property="Margin" Value="4"/>
        </Style>
    </ui:BaseRibbonWindow.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Text -->
        <TextBlock Grid.Column="2"
                   Margin="10,0,0,0"
                   Text="{Binding MessageText}"
                   TextWrapping="Wrap"
                   TextAlignment="{Binding MessageTextAlignment}"
                   MaxWidth="420"
                   VerticalAlignment="Center"
                   />

        <!-- Buttons -->
        <StackPanel Grid.Row="1"
                    Grid.ColumnSpan="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0"
                    >

            <!-- OK -->
            <Button Content="OK"
                    Visibility="{Binding OkVisibility}"
                    IsDefault="{Binding IsDefaultOk}"
                    Click="Ok_Click"
                    />

            <!-- Yes -->
            <Button Content="Yes"
                    Visibility="{Binding YesVisibility}"
                    IsDefault="{Binding IsDefaultYes}"
                    Click="Yes_Click"
                    />

            <!-- No -->
            <Button Content="No"
                    Visibility="{Binding NoVisibility}"
                    IsDefault="{Binding IsDefaultNo}"
                    Click="No_Click"
                    />

            <!-- Cancel -->
            <Button Content="Cancel"
                    Visibility="{Binding CancelVisibility}"
                    IsCancel="True"
                    IsDefault="{Binding IsDefaultCancel}"
                    Click="Cancel_Click"
                    />
        </StackPanel>
    </Grid>

</ui:BaseRibbonWindow>
